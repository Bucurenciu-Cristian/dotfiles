# Ansible managed: Ansible managed

[user]
    # --- Default (Personal) Configuration ---
    name = Bogdan Cristian Bucurenciu
	email = cristian.bucurenciu@sepa.at

[commit]
    # Enable commit signing globally
    gpgsign = true

[gpg]
    # Tell Git to use SSH format for signing
    format = ssh

[gpg "ssh"]
    # Tell Git how to use 1Password for signing
	program = /opt/1Password/op-ssh-sign
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
